# Multipass folder

This folder must contains passwords for the differents services.

Create following files in the folder : `minio_admin_password`, `minio_secret_key`, `minio_user_password`
and add a single line password string into each